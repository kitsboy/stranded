# Stranded — environment variables
NEXT_PUBLIC_SITE_URL=https://stranded.giveabit.io
BUILD_STATIC=true

# Optional: deploy notifications (GitHub Actions secret)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Git: use SSH instead of HTTPS tokens
# git remote set-url origin git@github.com:kitsboy/stranded.git